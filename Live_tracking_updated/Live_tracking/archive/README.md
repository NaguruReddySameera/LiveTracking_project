Archived components

The following legacy or duplicate items have been moved to this folder for safekeeping:

- `client/` — legacy React client duplicate
- `src/` — legacy Node/Express server (server.js, routes, etc.)
- `Dockerfile` — legacy Node server Dockerfile
- `package.json.old` — old root package info

These items were archived to simplify the repository structure and make deployment easier. If you need to restore any of them, move them out of this folder and update `docker-compose.yml` and related docs accordingly.
